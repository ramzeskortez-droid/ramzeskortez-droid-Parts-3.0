
# Взаимодействие, Логика и Структуры Данных

## 1. Структура Данных
... (без изменений) ...

---

## 2. Жизненный Цикл Сделки

### Этап 1: Создание Заказа (Client)
1. Клиент нажимает "Отправить запрос".
2. Бэкенд (GAS) выполняет `addLeadWithTg`:
    *   **Переход на POST JSON:** Все запросы к Bitrix24 теперь используют `method: post` и передают данные в теле JSON. Это устраняет ошибки при длинных списках товаров.
    *   Создает лид в Битрикс24.
    *   **Возвращает `leadId`**.
3. Формируется уведомление в Telegram (ФИНАЛЬНЫЙ ФОРМАТ):
    *   **Заголовок:** 🔥 НОВЫЙ ЗАКАЗ.
    *   **Список позиций:** `[Название] ([Категория]) — [Количество] шт` (строго без цен, без нумерации).
    *   **CRM Ссылка:** Гиперссылка на лид, маскированная под название лида.

---

## 3. Backend Logic (Google Apps Script)
*   **Bitrix24 Integration:** Используются методы `crm.lead.add.json` и `crm.lead.productrows.set.json`.
