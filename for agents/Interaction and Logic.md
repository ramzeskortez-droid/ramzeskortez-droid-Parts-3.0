
# Взаимодействие, Логика и Структуры Данных

## 1. Логика Дашбордов Аналитики (Seller UI)
Дашборды в `SellerInterface.tsx` работают по принципу агрегации всех записей типа `ORDER`.

### Алгоритм расчета:
1.  **Парсинг даты:** Функция `parseRuDate` берет значение из колонки J. Она умеет обрабатывать форматы `23.12.2025` и `2025-12-23`, даже если внутри ячейки есть перенос строки (Alt+Enter в Excel).
2.  **Диапазон "Сегодня":** Включает заказы, где `timestamp` находится между `00:00:00` и `23:59:59` текущего дня.
3.  **Диапазон "Неделя/Месяц":** Скользящее окно в 7 и 30 дней назад от текущего момента.
4.  **Важно:** Если заказ имеет дату в будущем (например, декабрь 2025, а сейчас февраль 2025), он попадет **только** в дашборд "Всего", но не в "Сегодня", так как он за пределами `endOfToday`.

## 2. Статусы и Видимость (Лидер/Резерв)
Процесс выбора запчастей админом:
1.  **Выбор Лидера:** В `AdminInterface` админ назначает одну из позиций поставщика "Лидером". Это записывает статус в JSON оффера и обновляет колонку I.
2.  **Видимость для клиента:** Офферы становятся видимы клиенту **только после того**, как админ нажмет кнопку **"Утвердить КП"**. Это действие устанавливает `processed = 'Y'` (Колонка L) для родительского заказа.
3.  **Логика Клиента:** Вкладка "В обработке" у клиента показывает заказы, у которых есть хотя бы один оффер со статусом `visibleToClient = 'Y'`. Внутри заказа клиент видит только те детали, которые помечены как `ЛИДЕР`. Остальные предложения поставщиков остаются скрытыми ("Резерв").

## 3. Кнопка "Готов купить" и Telegram
Финальный этап воронки:
1.  **Действие:** Клиент нажимает "Готов купить".
2.  **Бэкенд:** В колонку M (`readyToBuy`) записывается `Y`. 
3.  **Telegram:** Бот отправляет сообщение с заголовком **🛍 КЛИЕНТ ГОТОВ КУПИТЬ**.
4.  **Состав уведомления:**
    *   ID заказа и Имя клиента.
    *   Список выбранных позиций (те, что были помечены `ЛИДЕР`).
    *   Цена за каждую позицию (AdminPrice).
    *   **Итоговая сумма:** Автоматический расчет суммы всех лидерских позиций.

## 4. Независимая Авторизация
*   Сессии `client_auth` и `seller_token` используют разные ключи в хранилище. 
*   Это позволяет одному пользователю одновременно быть авторизованным как Поставщик (под брендом "123") и как Клиент (под именем "Дмитрий"), не перетирая данные друг друга при переключении вкладок.
