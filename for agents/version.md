
# История версий проекта

## 1.6.8
- **Client UI:** Исправлена проблема с узкой колонкой статуса. Сетка расширена для корректного отображения бейджей «В ОБРАБОТКЕ».
- **Seller UI:** Восстановлены дашборды «НЕДЕЛЯ» и «МЕСЯЦ». Карточка «Лидер спроса» перенесена на отдельную строку для лучшей читаемости.
- **Logic:** Обновлен `marketStats` для расчета динамики за 7 и 30 дней.

## 1.6.7
- **Critical Fix:** Исправлена опечатка в импорте `lucide-center` в `ClientInterface.tsx`. Модуль переименован в `lucide-react`.
- **Client UI:** В таб-баре текст "Предложения" заменен на "В обработке" для синхронизации со статусами в списке.
...
